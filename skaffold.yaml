apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/kevinstl/lightning-kube-bitcoind:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/kevinstl/lightning-kube-bitcoind:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: lightning-kube-bitcoind
        chartPath: charts/lightning-kube-bitcoind
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/kevinstl/lightning-kube-bitcoind"
          image.tag: "{{.DIGEST_HEX}}"
